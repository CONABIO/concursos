<%= form_with(model: categoria, local: true) do |form| %>
  <% if categoria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoria.errors.count, "error") %> prohibited this categoria from being saved:</h2>

      <ul>
      <% categoria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre_categoria %>
    <%= form.text_field :nombre_categoria, id: :categoria_nombre_categoria %>
  </div>

  <div class="field">
    <%= form.label :cat_concurso_id %>
    <%= form.number_field :cat_concurso_id, id: :categoria_cat_concurso_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
